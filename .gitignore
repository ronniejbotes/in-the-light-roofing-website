node_modules/
dist/
.vite-out/
shots/
.routes.json
.probe.json
.assets-missing.json
*.log
.DS_Store
Thumbs.db
__pycache__/

# scratch diagnostics
tools/.tmp-*
mirror-test/

# Client audit report — contains live vulnerability detail. Never commit to a public repo.
audit-live-site.md

# Keyword map — client deliverable, keep local until reviewed
keyword-map.md

# Agents drop working files here; never part of the project
scratchpad/

# Built by build/publish.mjs; regenerate rather than commit (264MB).
publish/

# Scratch builds and preview logs from the SEO work
.out-*/
.serve-*.log
